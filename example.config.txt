# Configuration file for script

# Charging currents in microamperes (µA)
NORMAL_CURRENT=3000000   # Default normal charging current (recommend value 2000000,3000000,4000000)
TURBO_CURRENT=6000000    # Default turbo charging current (recommend values 5000000,5500000,6000000)

# Temperature threshold in millidegrees Celsius (m°C)
TEMP_THRESHOLD=430       # Temperature threshold for switching to normal mode (recommend values 40,41,42,43,44,45,46,47)

# Duration in seconds to maintain normal mode after temperature threshold is reached
TEMP_DURATION=30         # Duration for which normal mode is maintained (cooldown period recommended 30,45,60)

# Interval in seconds for checking temperature and charger status
INTERVAL=15              # Interval for checking temperature and charger status (don't recommend to change but if you do set interval to be divisible by cooldown period)
                           # should be 1/2 or less than Temp duration e.g 15 so temp duration 30,45 if 20 then 40,60 etc